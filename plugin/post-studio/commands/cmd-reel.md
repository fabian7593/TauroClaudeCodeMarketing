---
description: Generar un reel para redes sociales a partir de uno o varios videos existentes
---

Generá un reel usando el/los video(s): $ARGUMENTS

Usá el skill `reel-highlights` (te va a preguntar el modo — highlights, video completo, o fusión — si no quedó claro en el pedido). Si `$ARGUMENTS` está vacío, buscá en `<contentRoot>/assets/videos/` y preguntame cuál video usar antes de continuar. Si esa carpeta está vacía, pedime que suba un video primero — no busques videos en otro lado del proyecto.
